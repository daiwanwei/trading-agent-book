# trading-agent-book M4 Implementation Plan（第四部：交易記憶 + 第五部：策略研究）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成第四部〈交易記憶──平倉之後學什麼？〉（六檔）與第五部〈策略研究──Agent 如何進化？〉（七檔），全書內容主線收尾於「Agent 每天自動改進書裡這些工具」的 meta 終章。

**Architecture:** 純內容里程碑——重寫 `book/part4-memory/` 六個與 `book/part5-research/` 七個佔位頁。沿用 M1–M3 模式。第四、五部寫的是 repo 自身的系統（非外部流派），「原著 vs 實作」段不適用；取而代之的通則是 schema/狀態機/管線事實全部據實溯源。

**Tech Stack:** Markdown + Mermaid + 既有 `scripts/check_book.py` / `generate_appendix.py`、pytest（迴歸）。

**Spec:** `docs/specs/2026-08-07-trading-agent-gitbook-design.md` §4 第四、五部。前例：M1–M3 計畫。

## Global Constraints

- 語言：繁體中文敘事；術語、指令、檔名、artifact/state 名稱保留英文 backticks。
- **全形標點鐵則**：CJK 散文全形（，：；（））；code span、fenced block、URL、純英文括注半形。
- **計數鐵則**：任何計數陳述用 `rtk proxy grep` 或 python one-liner（本環境 `grep -c` 會被 hook 改寫）。
- **數字鐵則**：每個門檻/欄位名/狀態名/schema 鍵都必須能在列出的來源檔案找到。
- 上游 repo（READ-ONLY）：`~/Projects/wade/math/claude-trading-skills`；連結格式 `https://github.com/tradermonty/claude-trading-skills/blob/main/<path>`。
- 場景不虛構；重述不搬運；閘門語義鐵則（verdict script 算、執行人守）。
- House style：跨章引用「X.Y 節」/「第N部 X.Y 節」，禁止 chX.Y；beta/production 狀態照 skills-index 據實。
- 既有章節事實不得矛盾——特別注意：序章的 trade-memory-loop / monthly-performance-review 段落、序章 Mermaid 的 EDGE 虛線邊（`docs/backlog.md` 記載 Part 5 必須重新查核其語義——Task 12 有專門步驟）、第三部 3.4 的 `journal_destination` 通則、2.3 的 EP→2.6 handoff。
- SUMMARY.md 標題已鎖定；每章 H1 與 SUMMARY 條目一致；每章完成後 `python3 scripts/check_book.py` → `0 issue(s)`。
- 所有 commit 訊息結尾：`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

---

### Task 1: 第四部開場

**Files:**
- Modify: `book/part4-memory/README.md`（600–900 CJK 字）

素材：三個既有部開場 + `workflows/trade-memory-loop.yaml`（全文）。

- [ ] **Step 1: 閱讀素材**（注意 trade-memory-loop 的 `when_to_run`「每次平倉，全出或部分出」與 `when_not_to_run`「贏的單也不能跳過」——序章引過，重述勿抄）
- [ ] **Step 2: 寫開場**：場景（某個星期三下午，一筆單平倉了——賺錢的單。Agent 的下一個動作不是慶祝，是開檢討）→ 本部地圖（4.1 論點生命週期 → 4.2 事後檢討 → 4.3 教練 → 4.4 模型書；標題照 SUMMARY）→ 收束句：第一到三部是「做交易」，第四部是「從交易變成經驗」——第五部再把經驗變成新工具。
- [ ] **Step 3: 驗證與 commit**：check_book `0 issue(s)`；CJK 600–900。

```bash
git add book/part4-memory/README.md
git commit -m "feat(part4): opening scene and part map"
```

---

### Task 2: 4.1 論點的生命週期

**Files:**
- Modify: `book/part4-memory/01-thesis-lifecycle.md`（1,800–2,800 CJK 字，五段模板）

素材（全讀）：`skills/trader-memory-core/SKILL.md`、`references/thesis_lifecycle.md`、`references/field_mapping.md`、`assets/postmortem_template.md`；`workflows/swing-opportunity-daily.yaml` step 10 與 `kanchi-dividend-weekly.yaml` step 6（兩處已寫過的登記場景——回指）。

要點：
- 場景：step 10 登記論點的那一刻——thesis 檔案在 `state/theses/` 誕生。
- 方法論：為什麼交易要當「論點」管理（假說→驗證→結案）；狀態機 IDEA→ENTRY_READY→ACTIVE→PARTIALLY_CLOSED→CLOSED（+TERMINATED 等——照 thesis_lifecycle.md 實際狀態集，不要漏也不要多）；轉換規則與 guard。
- 機制：CLI 的 ingest/store/review 三入口；來源 adapter（哪些 screener 能直接 ingest——照 SKILL.md/thesis_ingest.py 據實）；trim/close/attach-position 的資料流；MAE/MFE 需 FMP（optional）據實；期貨論點的 asset_type 分派（CLAUDE.md 有記載——以上游 SKILL.md 為準驗證）。
- 判讀與誤用：「不到真實成交不進 ACTIVE」的鐵則（2.4/序章已立——引用深化）；thesis 記錄是給 4.2–4.4 吃的原料，記錄不誠實下游全毀。
- 延伸閱讀：4 份來源連結。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part4): ch4.1 thesis lifecycle`

---

### Task 3: 4.2 誠實的事後檢討

**Files:**
- Modify: `book/part4-memory/02-postmortem.md`（1,500–2,500 CJK 字，五段模板）

素材（全讀）：`skills/signal-postmortem/SKILL.md`、`references/outcome-classification.md`、`references/feedback-integration.md`；`workflows/trade-memory-loop.yaml`（step 2 的四類根因 gate——序章引過）。

要點：
- 場景：CLOSED 的 thesis 進了 postmortem。
- 方法論：outcome ≠ process（賺錢的爛單、虧錢的好單）；四類根因歸因（含 randomness 作為一等公民——照 YAML 與 outcome-classification.md 實際分類）；MAE/MFE 在檢討中的意義。
- 機制：signal-postmortem 的輸入（closed thesis record）→ 分類 → `postmortem_findings`；feedback-integration.md 的迴路（findings 怎麼回到規則——step 3 的 accept/modify/defer/journal-only gate）。
- 判讀與誤用：把 randomness 當藉口 vs 當真實類別的差異；「贏的單也要檢討」的 when_not_to_run 重述。
- 延伸閱讀：3 份來源 + trade-memory-loop.yaml 連結。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part4): ch4.2 honest postmortem`

---

### Task 4: 4.3 教練看的是行為不是損益

**Files:**
- Modify: `book/part4-memory/03-coach.md`（1,500–2,500 CJK 字，五段模板）

素材（全讀）：`skills/trade-performance-coach/SKILL.md`、`references/review-framework.md`、`references/behavior-tags.md`、`references/risk-review-checklist.md`、`references/output-contract.md`；`skills/weekly-performance-digest/SKILL.md`、`references/weekly-digest-metrics.md`；`workflows/monthly-performance-review.yaml`（Plan→Trade→Record→Review→Improve——序章引過）。

要點：
- 場景：月初第一個週末的 90 分鐘（序章場景的深化）。
- 方法論：行為模式 vs 單筆結果；behavior-tags 的實際標籤體系（照檔案實數——幾類、哪些）；週摘要（勝率/期望值——weekly-digest-metrics 實際指標）與月檢討的分工。
- 機制：coach 的輸入（thesis/postmortem/journal）→ 行為標籤 → `next_session_operating_rules`；digest 的 `weekly_performance_digest` 輸出；monthly-review 的 step 5（dual-axis-skill-reviewer 那步——一句預告 5.5）。
- 判讀與誤用：operating rules 是拿來遵守的不是收藏的；「刪掉沒用的規則」也是輸出（monthly manual_review 的 delete or downgrade——3.4 引過，重述勿抄）。
- 延伸閱讀：7 份來源連結。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part4): ch4.3 behavior coach`

---

### Task 5: 4.4 模型書：把熟練度變成資料

**Files:**
- Modify: `book/part4-memory/04-model-book.md`（1,500–2,500 CJK 字，五段模板）

素材（全讀）：`skills/stockbee-setup-fluency-trainer/SKILL.md`、`references/model_book_schema.md`、`references/outcome_tags.md`、`references/review_workflow.md`；`workflows/stockbee-fluency-loop.yaml`。

要點：
- 場景：2.3 的 momentum burst 候選，不管有沒有進場，都能進模型書。
- 方法論：Stockbee 的 setup fluency 概念（看過一千張圖的人反應不一樣）；把「熟練度」變成可累積資料的想法；3/5 日 forward outcome 與 MFE/MAE。
- 機制：JSONL model book 的 schema（照 model_book_schema.md 實際欄位）；outcome tags 體系（照實）；cohort statistics 與 rule_candidates 輸出；離線 OHLCV / FMP optional 據實。
- 判讀與誤用：模型書統計是樣本累積不是回測（樣本偏差警語照 references 據實）；beta 狀態誠實標注。
- 延伸閱讀：5 份來源連結。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part4): ch4.4 setup model book`

---

### Task 6: 第四部實作章

**Files:**
- Modify: `book/part4-memory/05-hands-on.md`（1,000–1,800 CJK 字，實作章格式）

**CARDINAL RULE**（同前兩部實作章）：所有指令與 `--help` 一致。入口：`trader-memory-core/scripts/trader_memory_cli.py`（頂層 + `ingest --help` + `store --help` + `review --help` 各看）；signal-postmortem / trade-performance-coach / weekly-performance-digest 的入口 script 從各 SKILL.md 找到後 `--help`。臨時 venv 可用；不打 live API；live-vs-docs 誠實表。

結構：兩種跑法 → trade-memory-loop checklist（close → postmortem → 反饋 gate；含一個完整的 close→postmortem 指令序列）→ 週/月節奏（digest 與 coach 的指令）→ 產出解讀（thesis YAML 片段與 `postmortem_findings` 形狀——欄位照 SKILL.md/schema 精確；SKILL.md 無 JSON 範例時從 script source 取，照第三部實作章前例並註明）→ 收尾（記錄誠實 = 下游一切的前提 + 立場聲明）。
- [ ] Steps: --help 蒐集 → 閱讀 → 寫章 → check_book + pytest + CJK → commit `feat(part4): hands-on chapter for the memory loop`

---

### Task 7: 第五部開場

**Files:**
- Modify: `book/part5-research/README.md`（600–900 CJK 字）

素材：四個既有部開場；`docs/backlog.md` 的 EDGE 待查項（知道它存在即可，結論由 Task 12 做）；CLAUDE.md（上游）的 Edge Research Pipeline 段。

- [ ] **Step 1: 閱讀素材**
- [ ] **Step 2: 寫開場**：場景（研究日——沒有開盤壓力的一天。第四部累積的記憶，今天要變成新的工具）→ 本部地圖（5.1 觀察→假說 → 5.2 假說→策略 → 5.3 審查與否證 → 5.4 管線自動化 → 5.5 Agent 改進自己；標題照 SUMMARY）→ 點出本部與全書的關係：這是循環閉合的地方。
- [ ] **Step 3: 驗證與 commit**：check_book；CJK 600–900。

```bash
git add book/part5-research/README.md
git commit -m "feat(part5): opening scene and part map"
```

---

### Task 8: 5.1 從觀察到假說

**Files:**
- Modify: `book/part5-research/01-observation-to-hypothesis.md`（1,500–2,500 CJK 字，五段模板）

素材（全讀）：`skills/edge-candidate-agent/SKILL.md`、`references/research_ticket_schema.md`、`references/signal_mapping.md`；`skills/edge-hint-extractor/SKILL.md`、`references/hints_schema.md`；`skills/trade-hypothesis-ideator/SKILL.md`、`references/hypothesis_types.md`、`references/evidence_quality_guide.md`。

要點：EOD 觀察 → tickets/anomalies/market_summary；hints.yaml 的 canonical 形狀；可否證假說卡（hypothesis_types 的實際類型；evidence quality 分級照實）；三個 skill 在管線中的位置（誰餵誰——照 SKILL.md 與上游 CLAUDE.md 管線圖據實）。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part5): ch5.1 observation to hypothesis`

---

### Task 9: 5.2 從假說到策略草稿

**Files:**
- Modify: `book/part5-research/02-hypothesis-to-strategy.md`（1,200–2,000 CJK 字，五段模板）

素材（全讀）：`skills/edge-concept-synthesizer/SKILL.md`、`references/concept_schema.md`；`skills/edge-strategy-designer/SKILL.md`、`references/strategy_draft_schema.md`。

要點：concept 的 thesis/invalidation/playbook 結構（schema 照實）；strategy draft variants 的生成；為什麼 concept 與 draft 要分兩層（抽象可重用 vs 具體可回測）。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part5): ch5.2 hypothesis to strategy draft`

---

### Task 10: 5.3 審查與否證

**Files:**
- Modify: `book/part5-research/03-review-and-falsification.md`（1,800–2,800 CJK 字，五段模板）

素材（全讀）：`skills/edge-strategy-reviewer/SKILL.md`、`references/review_criteria.md`、`references/overfitting_checklist.md`；`skills/backtest-expert/SKILL.md`（+自行 ls references/ 補讀相關檔）；`skills/residual-edge-analyzer/SKILL.md`、`references/methodology.md`；`skills/strategy-pivot-designer/SKILL.md`、`references/stagnation_triggers.md`、`references/pivot_techniques.md`。

要點：PASS/REVISE/REJECT 判準（review_criteria 照實）；過擬合檢查清單的實際條目；回測的樣本內外紀律（backtest-expert 據實）；殘差 edge 的 HAC 回歸拆解（methodology.md 據實——這是全書最技術的一段，寫給普通讀者聽得懂）；調參停滯偵測與結構性 pivot。本章是第五部的「紀律章」——與第三部閘門哲學同構（研究也有 fail-closed）。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part5): ch5.3 review and falsification`

---

### Task 11: 5.4 整條管線自動化

**Files:**
- Modify: `book/part5-research/04-pipeline-automation.md`（1,500–2,500 CJK 字，五段模板）

素材（全讀）：`skills/edge-pipeline-orchestrator/SKILL.md`、`references/pipeline_flow.md`、`references/revision_loop_rules.md`；`skills/stockbee-20pct-study/SKILL.md`、`references/methodology.md`、`references/catalyst_taxonomy.md`；`skills/edge-signal-aggregator/SKILL.md`、`references/signal-weighting-framework.md`（一段即可）。

要點：orchestrator 的階段串接與 REVISE 迴圈（max 2 cycles——照 revision_loop_rules 實數）；±20% 事件研究作為「持續餵管線的觀察來源」（明確不當買賣訊號——skills-index 據實）；aggregator 的加權去重一段。配 Mermaid 管線圖（真實 stage/artifact 名——照 pipeline_flow.md）。
- [ ] Steps: 閱讀 → 寫章 → check_book + CJK → commit `feat(part5): ch5.4 pipeline automation`

---

### Task 12: 5.5 Agent 改進自己的 skills（全書 meta 終章）

**Files:**
- Modify: `book/part5-research/05-self-improvement.md`（1,800–2,800 CJK 字，五段模板）

素材（全讀）：上游 `CLAUDE.md` 的「Skill Self-Improvement Loop」與「Skill Auto-Generation Pipeline」兩節（scripts/run_skill_improvement_loop.py、launchd 排程、quality gate 與 rollback 設計）；`skills/dual-axis-skill-reviewer/SKILL.md`、`references/scoring_rubric.md`；`skills/skill-designer/SKILL.md`、`references/quality-checklist.md`；`skills/skill-idea-miner/SKILL.md`、`references/idea_extraction_rubric.md`。

要點：
- 場景：凌晨 05:00，launchd 叫醒改進管線——書裡描述的每個工具，每天都被評分、被改進、被開 PR。
- 方法論：雙軸評分（deterministic auto 軸 vs LLM 軸——為何改進觸發用 auto 軸；照上游 CLAUDE.md 據實）；quality gate 再評分與 rollback；週挖掘（session logs → 點子 backlog）→ 日生成（設計→審查→PR）。
- 機制：兩條管線的實際檔案與狀態（logs/.skill_improvement_state.json 等——照上游 CLAUDE.md）；monthly-performance-review 的 `skill_improvement_backlog` artifact 是人工迴路的入口（4.3 預告過——收攏）。
- **Step 專項：序章 EDGE 虛線邊查核**（docs/backlog.md 的 M4 前置項）：對照上游事實，判定序章 Mermaid 的 `EDGE -.-> SOD`（改進後的 skills → swing-opportunity-daily）與「skill improvement loop vs edge pipeline 敘事順序」是否需要修正序章。結論寫進報告：CONFIRMED（序章現狀可留）或 NEEDS_FIX（給出具體修正建議，由最終審查 fix wave 執行——本任務不改序章）。
- 判讀與誤用：自動改進的邊界（PR 仍需人 merge——閘門語義的最後一次呼應）；全書收束段：讀者手上這本書描述的系統，明天早上又會比今天好一點。
- 延伸閱讀：來源連結（上游 CLAUDE.md 連結用 blob/main/CLAUDE.md）。
- [ ] Steps: 閱讀 → 寫章（含 EDGE 查核）→ check_book + CJK → commit `feat(part5): ch5.5 self-improvement meta finale`

---

### Task 13: 第五部實作章

**Files:**
- Modify: `book/part5-research/06-hands-on.md`（1,000–1,800 CJK 字，實作章格式）

**CARDINAL RULE**：指令與 `--help` 一致。入口：`edge-pipeline-orchestrator/scripts/orchestrate_edge_pipeline.py`（--help；上游 CLAUDE.md 有 full/review-only/dry-run 三種模式的範例——以 --help 驗證後採用）；各 subskill 的入口 script 從 SKILL.md 找到後至少驗證 edge-candidate-agent 與 edge-strategy-reviewer 兩個（其餘可引 orchestrator 一鍵模式帶過）。臨時 venv 可用；不打 live API。

結構：兩種跑法 → 管線 checklist（手動逐段 vs orchestrator 一鍵；--dry-run 先跑一次的建議）→ 前置需求（幾乎全離線、FMP optional 據實）→ 產出解讀（review.yaml 的 PASS/REVISE/REJECT 與 pipeline_report 形狀照實）→ 收尾（研究產出 ≠ 可上線策略——export eligible 的條件一句 + 立場聲明）。
- [ ] Steps: --help 蒐集 → 閱讀 → 寫章 → check_book + pytest + CJK → commit `feat(part5): hands-on chapter for the edge pipeline`

---

### Task 14: M4 收尾

**Files:**
- Modify: `book/appendix/a-skills-reference.md`、`b-workflows.md`（重新生成）；`README.md`（進度表）

- [ ] **Step 1**: `python3 scripts/generate_appendix.py` → `git diff --stat` 報告變動性質
- [ ] **Step 2**: `python3 -m pytest tests/ -q` → 12 passed；`python3 scripts/check_book.py` → `0 issue(s)`
- [ ] **Step 3**: README 進度表：

```markdown
| 部分 | 狀態 |
|---|---|
| 序章 + 第一部 | ✅ 已完成 |
| 第三部（風險與紀律） | ✅ 已完成 |
| 第二部（五大流派） | ✅ 已完成 |
| 第四部（交易記憶）+ 第五部（策略研究） | ✅ 已完成 |
| 附錄 C/D + 全書校對（M5） | 🚧 最後一哩 |
```

- [ ] **Step 4**: `git add -A && git commit -m "chore: regenerate appendices and mark M4 complete"` + trailer，`git push -u origin build/m4`
