# trading-agent-book M3 Implementation Plan（第二部：選股──五大流派）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成第二部〈選股──做什麼？〉全部八個檔案：開場 + 五大流派章（Minervini / O'Neil / Stockbee / Kanchi / Shapiro）+ 事件驅動衛星章 + 實作章。全書最厚的一部。

**Architecture:** 純內容里程碑——重寫 `book/part2-screening/` 八個佔位頁。沿用 M1/M2 模式：五段章節模板（2.1–2.6）、逐條事實溯源、`check_book.py` 把關、實作章指令 `--help` 驗證。2.5 Shapiro 章配 Mermaid 管線圖。

**Tech Stack:** Markdown + Mermaid + 既有 `scripts/check_book.py` / `generate_appendix.py`、pytest（迴歸）。

**Spec:** `docs/specs/2026-08-07-trading-agent-gitbook-design.md` §4 第二部。前例：`docs/plans/2026-08-07-gitbook-m0-m1.md`、`2026-08-07-gitbook-m2.md`。

## Global Constraints

- 語言：繁體中文敘事；術語、指令、檔名、artifact 名稱保留英文 backticks。
- **全形標點鐵則（M2 教訓）**：CJK 散文一律全形標點（，：；（））；code span、fenced block、URL、純英文括注維持半形。寫完自查。
- **計數鐵則（M2 教訓）**：任何「N 個/N 條/所有」型陳述，計數一律用 `rtk proxy grep` 或 python one-liner（本環境的 `grep -c` 會被 hook 改寫輸出）。
- 上游 repo（READ-ONLY）：本機 `~/Projects/wade/math/claude-trading-skills`；連結格式 `https://github.com/tradermonty/claude-trading-skills/blob/main/<path>`。
- 場景不虛構：敘事只用 `workflows/*.yaml` 真實 step / gate / artifact 名稱。
- 重述不搬運：不整段翻譯；引用以繁中重述 + 連結。原著方法論（Minervini/O'Neil/Stockbee/Kanchi/Shapiro）出處標注以 references 實際引用為準——references 沒提的書名/來源不可自行補。
- 五段章節模板（2.1–2.6）：場景 → 方法論 → 機制 → 判讀與誤用 → 延伸閱讀。
- 數字鐵則：每個數值門檻/評分權重/公式都必須能在列出的來源檔案找到。
- 閘門語義鐵則：verdict 是 script 算的、執行是人守的；screener 輸出是候選生成不是買賣訊號（swing-opportunity-daily `manual_review` 原則）。
- 已成章節的既有定調不得矛盾：ch1.5/3.3 的閘門語義、ch3.1 的部位數字、ch3.2 的「step 7 無 CLI、圖表判讀由 Claude」、2.5 相關的 Shapiro 分工（step 3 = technical-analyst 已在 3.2 帶過、step 4 = futures-position-sizer 已在 3.1 詳述——2.5 負責整條管線的敘事，個別步驟回指即可）。
- SUMMARY.md 第二部標題已鎖定；每章 H1 與 SUMMARY 條目一致。
- 每章完成後：`python3 scripts/check_book.py` → `0 issue(s)`。
- 所有 commit 訊息結尾：`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`。

---

### Task 1: 第二部開場

**Files:**
- Modify: `book/part2-screening/README.md`（重寫佔位，600–900 CJK 字）

**Interfaces:**
- Consumes: `book/prologue/agent-week.md`、`book/part1-regime/README.md`、`book/part3-discipline/README.md`（部開場的既有寫法）；上游 `workflows/swing-opportunity-daily.yaml` steps 2–6
- Produces: 「五大流派」的正式命名與一句話定位（各章開場回扣）；本部與第一/三部的關係句（問市場→做什麼→問自己）

- [ ] **Step 1: 閱讀素材**

讀三個既有部開場 + swing-opportunity-daily.yaml steps 2–6（step 2 vcp 必跑、steps 3–6 optional 的結構）。注意第三部開場已建立「第一部問市場、第二部問標的、第三部問自己」的句式——本部開場要接住。

- [ ] **Step 2: 寫開場**

結構：
- **場景**：07:35，`exposure_decision` 是 allow、`circuit_breaker_decision` 是 `TRADING_ALLOWED`——現在才輪到「找標的」。steps 2–6 是五個平行的偵查隊。
- **本部地圖**：2.1 Minervini（VCP 與突破）→ 2.2 O'Neil（CANSLIM）→ 2.3 Stockbee（動能三部曲）→ 2.4 Kanchi(存股紀律）→ 2.5 Shapiro（逆勢管線，唯一不在 swing-opportunity-daily 裡、自成一條 workflow 的流派）→ 2.6 事件驅動衛星。標題照 SUMMARY。點明：流派之間不是互斥選項，是不同市場狀態下的不同武器。
- 收尾預告實作章（steps 2–6 實跑）。

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；CJK 600–900。

```bash
git add book/part2-screening/README.md
git commit -m "feat(part2): opening scene and part map"
```

---

### Task 2: 2.1 Minervini：波動收縮與突破

**Files:**
- Modify: `book/part2-screening/01-minervini.md`（重寫佔位，1,800–2,800 CJK 字）

**Interfaces:**
- Consumes: 上游 `skills/vcp-screener/SKILL.md` + `references/vcp_methodology.md` + `references/scoring_system.md`；`skills/breakout-trade-planner/SKILL.md` + `references/minervini_entry_rules.md`；`workflows/swing-opportunity-daily.yaml` steps 2、9
- Produces: 「流派章」寫法慣例——原著方法 vs repo 程式化實作的區分段（2.2–2.5 沿用）

- [ ] **Step 1: 閱讀上述 7 份素材**

- [ ] **Step 2: 依五段模板寫章**

1. **場景**：step 2——五個偵查隊裡唯一的必跑者。`vcp_candidates` 的誕生。
2. **方法論**：Minervini 的 VCP——波動逐段收縮、量能遞減、pivot 突破（照 vcp_methodology.md 的實際敘述與出處標注；SEPA/Stage 分析如有提及照實寫）。**原著 vs 實作**：spec §6 要求點出 repo 的程式化實作對原著做了哪些量化簡化（照 SKILL.md/scoring_system.md 據實比對——例如收縮段數、量能條件的具體閾值化）。
3. **機制**：vcp-screener 篩 S&P 500（FMP OHLCV）→ 收縮偵測 → 評分（scoring_system.md 的實際權重）→ `vcp_candidates` + pivot 位；breakout-trade-planner（step 9, optional）把 `validated_setups` 變成含 worst-case 風險與 Alpaca 單模板的 `trade_plans`（minervini_entry_rules.md 的進場規則；注意 M2 實作章已確認其 `--input` 收 VCP screener JSON——不要與 YAML 的 consumes 混述，照 ch3.3/實作章的處理方式）。
4. **判讀與誤用**：screener 是候選生成不是訊號（manual_review 原則回扣）；VCP 分數高 ≠ 一定突破；突破失敗的處理屬第三部風險章的範疇（一句回指）。
5. **延伸閱讀**：5 份上游檔案連結。

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；CJK 1,800–2,800。

```bash
git add book/part2-screening/01-minervini.md
git commit -m "feat(part2): ch2.1 Minervini VCP and breakout"
```

---

### Task 3: 2.2 O'Neil：CANSLIM 成長股

**Files:**
- Modify: `book/part2-screening/02-canslim.md`（重寫佔位，1,500–2,500 CJK 字）

**Interfaces:**
- Consumes: 上游 `skills/canslim-screener/SKILL.md` + `references/canslim_methodology.md` + `references/scoring_system.md` + `references/interpretation_guide.md`；`workflows/swing-opportunity-daily.yaml` step 5
- Produces: 無（獨立流派章）

- [ ] **Step 1: 閱讀上述 5 份素材**

- [ ] **Step 2: 依五段模板寫章**

1. **場景**：step 5（optional）——當 Agent 想用基本面過濾動能。
2. **方法論**：CANSLIM 七字母各代表什麼（照 canslim_methodology.md 實際定義與數值門檻；O'Neil《How to Make Money in Stocks》出處如 references 有提照實標）。與第一部的血緣：O'Neil 的市場面（M = Market Direction）正是 1.2/1.3 的派發日與 FTD——一段講清楚這個跨部呼應。
3. **機制**：canslim-screener 的 FMP 基本面資料流 → 各字母的量化評分（scoring_system.md 實際權重）→ `canslim_candidates` 與 composite scores。
4. **判讀與誤用**：interpretation_guide.md 的實際判讀建議；基本面數據的滯後性；七項全滿的股票稀有——分數的相對性。
5. **延伸閱讀**：4 份上游檔案連結。

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；CJK 1,500–2,500。

```bash
git add book/part2-screening/02-canslim.md
git commit -m "feat(part2): ch2.2 O'Neil CANSLIM"
```

---

### Task 4: 2.3 Stockbee：動能三部曲

**Files:**
- Modify: `book/part2-screening/03-stockbee.md`（重寫佔位，2,000–3,000 CJK 字——三個 skill 的合章）

**Interfaces:**
- Consumes: 上游 `skills/stockbee-momentum-burst-screener/SKILL.md` + `references/momentum_burst_methodology.md` + `references/entry_exit_rules.md`；`skills/stockbee-episodic-pivot-analyzer/SKILL.md` + `references/ep_methodology.md` + `references/catalyst_quality.md` + `references/handoff_rules.md`；`skills/stockbee-exhaustion-hammer-screener/SKILL.md` + `references/exhaustion_hammer_methodology.md`；`workflows/swing-opportunity-daily.yaml` steps 3–4；`workflows/stockbee-ep-daily.yaml`
- Produces: 無

- [ ] **Step 1: 閱讀上述 10 份素材**

- [ ] **Step 2: 依五段模板寫章**

1. **場景**：steps 3–4（optional）同時出動的兩支 Stockbee 偵查隊，加上自成一條 workflow 的 EP（stockbee-ep-daily）。
2. **方法論**：Stockbee（Pradeep Bonde）的三種武器與適用時機：動能爆發（3–5 天 momentum burst，4% 突破/金額突破/量能擴張——照 methodology 實數）、EP（Day 1 episodic pivot，催化劑品質分類——catalyst_quality.md 的實際類別）、賣壓耗竭鎚（undercut/reclaim + 鎚形幾何——照 methodology 實數）。出處標注照 references 實際引用。
3. **機制**：三個 screener 各自的輸入→評分→輸出 artifact（`momentum_burst_candidates`、`episodic_pivot_candidates`、`exhaustion_hammer_candidates`）；EP 的 handoff_rules（delayed-EP watchlist、PEAD handoff——與 2.6 的接口一句預告）；市場 regime gate 與 risk-distance 過濾的共通設計。
4. **判讀與誤用**：workflow manual_review 對兩個 Stockbee screener 的明文限制（candidate generation only + 各自的額外檢查——M1 序章已引過，重述勿抄）；EP 需要催化劑事件輸入、skill 不自己找新聞（skills-index 據實寫）。
5. **延伸閱讀**：9 份上游檔案連結。

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；CJK 2,000–3,000。

```bash
git add book/part2-screening/03-stockbee.md
git commit -m "feat(part2): ch2.3 Stockbee momentum trilogy"
```

---

### Task 5: 2.4 Kanchi：存股的紀律

**Files:**
- Modify: `book/part2-screening/04-kanchi.md`（重寫佔位，1,800–2,800 CJK 字）

**Interfaces:**
- Consumes: 上游 `skills/kanchi-dividend-sop/SKILL.md` + `references/default-thresholds.md` + `references/valuation-and-one-off-checks.md`；`skills/value-dividend-screener/SKILL.md` + `references/screening_methodology.md`；`skills/dividend-growth-pullback-screener/SKILL.md` + `references/rsi_oversold_strategy.md` + `references/dividend_growth_compounding.md`；`workflows/kanchi-dividend-weekly.yaml`
- Produces: 無

- [ ] **Step 1: 閱讀上述 9 份素材**

- [ ] **Step 2: 依五段模板寫章**

1. **場景**：離開日內節奏——週六上午的 kanchi-dividend-weekly（序章的週末段落已帶過場景，這裡深化：這是唯一以「持有十年」為預設的流派）。
2. **方法論**：Kanchi 流派是誰、五步 SOP 的實際步驟（照 SKILL.md；Kanchi 的身分描述照 references 據實寫，不腦補）；兩個輔助 screener 的定位：value-dividend（P/E<20、P/B<2、殖利率≥3%、三年成長——照 screening_methodology 實數）與 dividend-growth-pullback（股息成長 12%+、RSI≤40——照 rsi_oversold_strategy 實數）。
3. **機制**：三個 skill 的資料流（FMP recommended）；default-thresholds.md 的實際門檻表；kanchi-dividend-weekly 的 step 結構與 gate（M1 序章驗證過的 CLEAN-PASS/…/FAIL 分級與「不到真實成交不進 ACTIVE」規則——重述勿抄）。
4. **判讀與誤用**：高殖利率陷阱（valuation-and-one-off-checks.md 的一次性項目檢查）；回檔進場 ≠ 接刀（RSI 條件的意義）；這一流派的賣出紀律在 review-monitor（T1–T5），屬核心組合管理、不在本書主線——一句帶過即可。
5. **延伸閱讀**：8 份上游檔案連結。

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；CJK 1,800–2,800。

```bash
git add book/part2-screening/04-kanchi.md
git commit -m "feat(part2): ch2.4 Kanchi dividend discipline"
```

---

### Task 6: 2.5 Shapiro：站在人群的對面（本部核心章）

**Files:**
- Modify: `book/part2-screening/05-shapiro.md`（重寫佔位，2,200–3,200 CJK 字）

**Interfaces:**
- Consumes: 上游 `skills/cot-contrarian-detector/SKILL.md` + `references/shapiro-methodology.md` + `references/cot-index-calculation.md`；`skills/news-reaction-failure-analyzer/SKILL.md` + `references/news-failure-patterns.md`；`skills/contrarian-setup-gate/SKILL.md` + `references/gate-decision-table.md`；`workflows/shapiro-contrarian.yaml`（全文——四步管線的敘事骨架）
- Produces: 無（本部收束前的高潮章）

- [ ] **Step 1: 閱讀上述 8 份素材**

注意兩個回指點（不重講）：step 3 價格行為確認 = technical-analyst 的 Shapiro 模式（ch3.2 已釐清 script 備援分工）；step 4 部位計算 = futures-position-sizer（ch3.1 已詳述）。本章的工作是把四步串成一條完整的逆勢敘事。

- [ ] **Step 2: 依五段模板寫章**

1. **場景**：週日，COT 報告出爐——這條管線的節奏是週級的，跟其他流派的日級不同。
2. **方法論**：Jason Shapiro 的逆勢邏輯（照 shapiro-methodology.md 據實寫，含出處）：擁擠部位 → 利多不漲的背離 → 價格行為確認 → 才有交易。為什麼「人群錯的時候才進場」需要四步而不是一步。COT index 的計算（cot-index-calculation.md 實際公式與門檻）。
3. **機制**：四步管線各自的輸入輸出（`cot_crowding_report` → `news_failure_verdict`（Monte-Carlo drift 檢定照 SKILL.md 據實寫）→ `price_action_confirmation_report` → contrarian-setup-gate 的 fail-closed 狀態機收斂成 `setup_status`（gate-decision-table.md 的實際決策表；READY_FOR_PLAN 等狀態名照實)）。配 **Mermaid 管線圖**（真實 artifact 名做邊、四個 skill 做節點、gate 標出）。與第三部的關係：這條管線自帶決策中樞（setup gate），與 swing-opportunity-daily 的閘門段平行——不是繞過紀律，是同構的另一實例。
4. **判讀與誤用**：逆勢不是唯反向（無確認不進場——fail-closed 的又一實例）；COT 資料的頻率限制（週更）與 FMP Premium+ 需求據實寫；beta 狀態的 skill 誠實標注（skills-index status 照實）。
5. **延伸閱讀**：7 份上游檔案連結。

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；CJK 2,200–3,200。

```bash
git add book/part2-screening/05-shapiro.md
git commit -m "feat(part2): ch2.5 Shapiro contrarian pipeline"
```

---

### Task 7: 2.6 事件驅動衛星

**Files:**
- Modify: `book/part2-screening/06-event-driven.md`（重寫佔位，1,500–2,500 CJK 字）

**Interfaces:**
- Consumes: 上游 `skills/earnings-trade-analyzer/SKILL.md` + `references/scoring_methodology.md`；`skills/pead-screener/SKILL.md` + `references/pead_strategy.md` + `references/entry_exit_rules.md`；`skills/theme-detector/SKILL.md` + `references/theme_detection_methodology.md`；`skills/parabolic-short-trade-planner/SKILL.md` + `references/parabolic_short_methodology.md`（進階選讀段）
- Produces: 無（本部最後內容章）

- [ ] **Step 1: 閱讀上述 8 份素材**

- [ ] **Step 2: 依五段模板寫章**

1. **場景**：財報季的早晨——事件驅動的機會窗跟流派無關，跟日曆有關。
2. **方法論**：財報動能鏈：earnings-trade-analyzer 的 5 因子（Gap/趨勢/量/MA200/MA50——scoring_methodology 實際權重）→ PEAD（財報後漂移的學理與 red-candle-pullback→breakout 形態，照 pead_strategy 據實寫）；theme-detector 的主題熱度邏輯；parabolic short 的反面視角（做空耗竭——一段進階選讀，明確標注三階段管線屬進階內容、細節留給上游文件）。
3. **機制**：Mode A/B 的管線接法（pead-screener 吃 earnings-trade-analyzer 輸出——SKILL.md 據實寫）；theme-detector 的 FINVIZ 資料流；各自的輸出 artifact。
4. **判讀與誤用**：事件驅動的樣本偏差（財報驚喜的不可預測性）；PEAD 的 entry_exit_rules 實際規則；放空的不對稱風險（parabolic_short 據實一段）。
5. **延伸閱讀**：8 份上游檔案連結。

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；CJK 1,500–2,500。

```bash
git add book/part2-screening/06-event-driven.md
git commit -m "feat(part2): ch2.6 event-driven satellites"
```

---

### Task 8: 第二部實作章

**Files:**
- Modify: `book/part2-screening/07-hands-on.md`（重寫佔位，1,000–1,800 CJK 字）

**Interfaces:**
- Consumes: 上游 scripts 的實際 CLI；`workflows/swing-opportunity-daily.yaml` steps 2–6；`book/part1-regime/06-hands-on.md` 與 `book/part3-discipline/05-hands-on.md`（實作章格式）
- Produces: 無

- [ ] **Step 1: 取得實際指令（鐵則：與 --help 一致；缺依賴可建臨時 venv；報告誠實記錄 live vs docs）**

各 skill 的入口 script 先從 SKILL.md 的 usage/quick-start 確認（已知：`stockbee-momentum-burst-screener/scripts/screen_momentum_burst.py`、`stockbee-exhaustion-hammer-screener/scripts/screen_exhaustion_hammer.py`、`theme-detector/scripts/theme_detector.py`；vcp-screener 與 canslim-screener 的主 script 以 SKILL.md 為準找到後 --help）。全部跑 `--help` 抄錄真實參數。不得對 live API 執行（--help only）。

- [ ] **Step 2: 寫實作章**

結構（沿用前兩部實作章格式）：
- **兩種跑法**：Claude Code 觸發 vs 直接跑 scripts
- **篩選段 checklist**：step 2（必跑）→ 3 → 4 → 5 → 6（各標 optional），每步指令 + 預期 artifact + 一句「這隊在找什麼」
- **前置需求**：這一段幾乎都要 FMP（各 SKILL.md 據實）；FINVIZ optional 的據實標注（theme-detector）
- **產出解讀**：候選清單的共通結構（symbol/score/setup 欄位——照各 SKILL.md 輸出範例，欄位名精確）；候選 ≠ 訊號的再次強調
- **收尾**：接第三部——候選離開這裡之後要過 step 7–11 的閘門段（回指 3 部實作章）+ 立場聲明

- [ ] **Step 3: 驗證與 commit**

Run: `python3 scripts/check_book.py` → `0 issue(s)`；`python3 -m pytest tests/ -q` → 12 passed；CJK 1,000–1,800。

```bash
git add book/part2-screening/07-hands-on.md
git commit -m "feat(part2): hands-on chapter for the screener sequence"
```

---

### Task 9: M3 收尾

**Files:**
- Modify: `book/appendix/a-skills-reference.md`、`book/appendix/b-workflows.md`（重新生成）
- Modify: `README.md`（進度表更新）

- [ ] **Step 1: 重新生成附錄**

Run: `python3 scripts/generate_appendix.py` → `git diff --stat` 檢視，報告變動性質。

- [ ] **Step 2: 全面驗證**

Run: `python3 -m pytest tests/ -q` → 12 passed；`python3 scripts/check_book.py` → `0 issue(s)`。

- [ ] **Step 3: README 進度表更新**

```markdown
| 部分 | 狀態 |
|---|---|
| 序章 + 第一部 | ✅ 已完成 |
| 第三部（風險與紀律） | ✅ 已完成 |
| 第二部（五大流派） | ✅ 已完成 |
| 第四、五部（M4）→ 附錄 C/D（M5） | 🚧 依序進行中 |
```

- [ ] **Step 4: Commit 與推送**

```bash
git add -A
git commit -m "chore: regenerate appendices and mark M3 complete"
git push -u origin build/m3
```
